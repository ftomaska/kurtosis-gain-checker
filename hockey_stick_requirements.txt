numpy
scipy
matplotlib
pillow
tifffile
# optional, only needed for the raw-TIFF NormCorre fallback (no reg_tif export found):
# CaImAn's compiled deps mean mamba/conda is the only supported install path (pip is not):
#   conda install -n base -c conda-forge mamba
#   mamba create -n caiman caiman
#   conda activate caiman
# On Windows, if conda itself fails with "SSL module is not available", that's a
# broken-DLL issue, not a real cert problem: copy libcrypto-1_1-x64.dll,
# libcrypto-1_1-x64.pdb, libssl-1_1-x64.dll, libssl-1_1-x64.pdb from
# <conda_root>\envs\<env>\Library\bin\ into <conda_root>\envs\<env>\DLLs\
